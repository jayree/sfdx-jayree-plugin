{
  "commandDescription": "make configuration changes that are not covered by the metadata API\nSee example configuration on how to define tasks",
  "tasks": "list of task titles, if no tasks are specified, all tasks marked as active will be executed",
  "concurrent": "execute tasks in parallel"
}
